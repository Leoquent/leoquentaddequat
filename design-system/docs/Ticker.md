---
category: Display
---

Full-width lime marquee band (48px) between sections — mono uppercase tech terms separated by dimmed slashes.

```tsx
<Ticker items={["GENERATIVE UI", "COMPUTER VISION", "PREDICTIVE MODELS", "NEURAL NETWORKS", "AUTONOMOUS AGENTS", "DATA PIPELINES"]} />
```

- Decorative texture, not navigation — it is aria-hidden and stops under prefers-reduced-motion.
- Use 5–8 short uppercase terms so the loop feels dense.
